info:
  name: Auth Profile
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4001/auth/login
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "User@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
