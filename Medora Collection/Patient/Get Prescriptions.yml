info:
  name: Get Prescriptions
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:4000/api/patients/patient/prescriptions?page=1&limit=10&status=active
  auth:
    type: bearer
    token: { { PATIENT_JWT_TOKEN } }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
