info:
  name: Get Medical History
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:4000/api/patients/patient/history?page=1&limit=20&status=open
  auth:
    type: bearer
    token: { { PATIENT_JWT_TOKEN } }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
