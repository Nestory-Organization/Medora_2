info:
  name: Patient Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4001/auth/login
  body:
    type: json
    data: |-
      {
        "email": "patient_test_123@test.com",
        "password": "Test@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
