info:
  name: Delete Document
  type: http
  seq: 15

http:
  method: DELETE
  url: http://localhost:4000/api/patients/patient/documents/{{documentId}}
  auth:
    type: bearer
    token: { { PATIENT_JWT_TOKEN } }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
