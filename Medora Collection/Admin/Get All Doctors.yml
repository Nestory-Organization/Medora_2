info:
  name: Get All Doctors
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api/admin/doctors
  body:
    type: none
  auth:
    type: bearer
    token: {{adminToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
