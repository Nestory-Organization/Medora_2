info:
  name: Admin Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@medora.com",
        "password": "admin123"
      }
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
