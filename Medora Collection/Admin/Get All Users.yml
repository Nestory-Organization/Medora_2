info:
  name: Get All Users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/admin/users
  body:
    type: none
  auth:
    type: bearer
    token: {{adminToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
