info:
  name: Verify Doctor Profile
  type: http
  seq: 2

http:
  method: PATCH
  url: http://localhost:4000/api/admin/doctor/:doctorId/verify
  body:
    type: json
    data: |-
      {
        "status": true
      }
  auth:
    type: bearer
    token: {{adminToken}}

params:
  path:
    doctorId: 
  query: []

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
