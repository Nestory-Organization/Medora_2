info:
  name: Verify doctor
  type: http
  seq: 3

http:
  method: PUT
  url: |-
    	
    http://localhost:4000/api/admin/verify-doctor
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
